{
  "name": "pipeline",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.9.8",
  "engines": { "node": ">=22" },
  "scripts": {
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "build": "pnpm -r build"
  }
}
